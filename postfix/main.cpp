#include <iostream>
#include<bits/stdc++.h>

using namespace std;

int main()
{
    stack<int>s;
    stack<int>::iterator it;
    char c;
    for(int i=0;i<5;i++){
        cin>>c;
        s.push(c);
    }
    for(it=s.begin();i<s.end();it++){
        if(it==* || )
    }
}
